## Pixcall AI Tagger 2.0.2

### 改进

- 首次打开插件且未设置模型目录时，直接打开原生文件夹选择器。
- 取消选择模型目录时显示轻量警告通知，不再弹出重复确认框。
- 运行 LLM 前检查 llamafile，缺少运行程序时直接引导下载。

### 安装

下载本页面的 `pixcall-plugin-v2.0.2.zip`，解压后在 Pixcall 插件管理器中选择解压得到的 `release-dist` 文件夹。
